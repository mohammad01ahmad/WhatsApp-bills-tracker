node_modules/
.env
.env.local
.next/
# glob, not an exact name — an `auth_session.bak` once slipped past a bare
# `auth_session/` rule in the sibling repo and got committed.
backend/auth_session*/
.DS_Store

.claude/
